{
  "name": "gmode-example-web-users",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build:deps": "../../../node_modules/.bin/turbo run build --filter=@gmode/core --filter=@gmode/service",
    "dev": "wrangler dev --ip 127.0.0.1 --port 8788",
    "deploy": "wrangler deploy",
    "predev": "pnpm build:deps",
    "pretypecheck": "pnpm build:deps",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gmode/core": "workspace:*",
    "@gmode/service": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260706.1",
    "typescript": "^5.6.3",
    "wrangler": "4.101.0"
  }
}
