{
  "name": "gmode-example-billing-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:deps": "../../../node_modules/.bin/turbo run build --filter=@gmode/core --filter=@gmode/rpc --filter=@gmode/service",
    "dev": "wrangler dev --ip 127.0.0.1 --port 8789",
    "deploy": "wrangler deploy",
    "pretypecheck": "pnpm build:deps",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gmode/core": "workspace:*",
    "@gmode/rpc": "workspace:*",
    "@gmode/service": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260706.1",
    "typescript": "^5.6.3",
    "wrangler": "4.107.0"
  }
}
