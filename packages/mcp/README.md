# @gmode/mcp

MCP catalog and operation tools for GMode gateways. Use it to expose aggregated
gateway operations to MCP-compatible clients.

See the root GMode README and `docs/mcp.md` for full usage guides.
