# @gmode/cli

GMode command-line tools for Cloudflare API Shield workflows, including schema
upload, discovery diffing, schema actions, and sequence exports.

The command binary is `gmode`. See `docs/api-shield.md` for full usage guides.
