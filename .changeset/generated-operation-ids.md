---
"@gmode/service": patch
---

Generate a stable `operationId` (e.g. `getUsersId`) for routes that omit one, so MCP tools and client codegen cover every route. Also corrected TSDoc defaults for `docs.internalOpenapi` and the gateway `defaults.auth` comment.
