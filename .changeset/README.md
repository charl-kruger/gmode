# Changesets

Run `pnpm changeset` for any public package change. Select the affected
`@gmode/*` packages and write a short release note that explains the user-facing
change.

The packages are fixed together while GMode is pre-1.0, so a release PR bumps
the public packages as a coherent set.
