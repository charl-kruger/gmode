---
"@gmode/gateway": minor
---

cloudflareRateLimit default key no longer references unmatched service; gateway default scopes/permissions are now enforced; middleware header handling is immutable-response safe.
